{
  "name": "insightsflow-backend",
  "version": "1.0.0",
  "description": "Backend de InsightsFlow",
  "main": "main.py",
  "scripts": {
    "dev": "python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python3 -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "install-deps": "pip3 install -r requirements.txt",
    "build": "echo 'Build completado'",
    "test": "echo 'No hay tests configurados'"
  },
  "keywords": ["python", "backend", "insightsflow"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  }
} 